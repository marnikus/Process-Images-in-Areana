/* sash-grid-drag.js — facade (Issue 4 split)

Now split into drag-core / drag-spec / drag-resize, each ≤250 LOC.
This file is kept for backwards compat but loads nothing — index.html loads the split files.
*/
