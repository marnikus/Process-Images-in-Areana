"""Characterization goldens for the run pipeline (Area A1).

Pins the batch behavior as structural traces so the A4 flip
(legacy `_do_run_batch` -> `batch_orchestrator.run_batch`) is provably
behavior-preserving. See `docs/archive/2026-09-19-area-a-bridge-pipeline/`.
"""
