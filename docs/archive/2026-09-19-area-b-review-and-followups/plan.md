# Area B Follow-up Plan — B7–B12 (2026-09-19)

From `review.md` findings F-1…F-7. Numbering continues Area B (rules numbers are stable, append only).
Scope guard: B-round follow-ups only — no Area A/C/D work (bridge, cdp_client/cdp_arena splits, watcher, coverage ramp to 80/75 global, mutation). Each step: tests first where behaviour could move (RULE 8), ≤20 LOC functions (RULE 18), RULE 19 order trivial (no new decisions — tests, wiring, doc, tooling).

| Step | Finding | Deliverable | Priority | Exit criteria |
|---|---|---|---|---|
| B7 | F-1 | Coverage completion for Area-B-owned modules: new real tests for `preset_store` (list/save/load/delete per family, default-fill, corrupt/non-dict fallback), `undo_store` (push/undo/redo/clamp/MAX_HISTORY/corrupt), `config_manager` (session round-trip, window presets, state merge), `site_adapter` (KeyError path), `output_state` (`flatten_diagnostics` non-dict/defaults/preserved), `cooldown_store` (idle expiry, persistable, page entry), `json_store` (unlink-failure branch) | P0 | every Area-B-owned module ≥80% line / ≥75% branch; global never decreases; each test fails if its target is deleted |
| B8 | F-2 | Vulture whitelist hygiene: rename Qt-shim params to distinctive names (`*sig_args/**sig_kwargs`, `*slot_args/**slot_kwargs`) in `cdp_client.py`, `bridge.py`, `captcha_recordings_bridge.py`; whitelist only those names; add `tests/test_vulture_clean.py` locking `vulture app tools/vulture_whitelist.py --min-confidence 90` = 0 findings | P0 | vulture @90 clean AND lock test green; shim files still import headless |
| B9 | F-3 | RULE 21 residual hardening: `_submit_fallback` literal → `probe_selectors.send_presence_selector()` (byte-identical string); `tests/test_action_block_defaults.py` — every live-element default (prompt/send/attach/preview) ∈ site_adapter inventory + JS `getDefaultBlocks()` mirror lock + frozen documented list of known legacy-divergent defaults (CHECK_SECURITY Playwright syntax etc., pending Area A RULE 10 decision); DOM_SELECTORS §E pointer + §I `presenceSelector` + §F note | P1 | no live selector literal outside site_adapter; consistency test green; docs aligned |
| B10 | F-4 | output_probes residual dedup, characterization FIRST: add node test for the pool-empty mismatch variant (visualNext edge) + key-set lock for both mismatch returns; then extract `assocShape(j)` + shared mismatch core inside the payload | P2 | jscpd output_probes internal clones 2→0; all 7 node scenarios green; payload field sets unchanged |
| B11 | F-5 | Stale docs sweep (RULE 17): superseded-header on `action-blocks-restore-plan.md` (unindexed + names deleted module as current) and `implementation_plan.md`; index rows in `docs/README.md`; SOR §8 rows for new test files | P1 | no doc outside archive claims deleted modules are current |
| B12 | F-6 | Gate honesty: `verify_quality.py --base <ref>` + loud warning (no silent all-files fallback) when merge-base missing; coverage ratchet — store `{line, branch}` in `quality_baseline.json`, `--coverage-ratchet` fails only on decrease (absolute 80/75 stays the final D4 target, warning mid-round); `pre_push_check.sh` picks `.venv/bin/python` and uses the ratchet; subprocess tests for both behaviours; final full recheck + record append | P1 | `bash tools/pre_push_check.sh` green end-to-end at current 43/34 coverage; no silent fallbacks; record updated |

Order: B7 → B8 → B9 → B11 → B10 → B12 (exit-criteria gaps and gate integrity first; B10 last among code steps because it touches a behaviour-locked payload).

Explicitly NOT here (owner areas): bridge/undo clone cluster (A5/A7), cdp_client API-surface vulture @60 candidates (C2), preset_store method split (C6), global coverage 80/75 (D4), `--allow-legacy` ratchet per-metric (R0.3/D6 full form — B12 ships only the minimal coverage ratchet + base flag).
