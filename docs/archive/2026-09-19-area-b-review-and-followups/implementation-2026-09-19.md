# Area B Follow-ups B7–B12 — Implementation Record (2026-09-19)

Execution of `plan.md` (same directory), in the planned order
B7 → B8 → B9 → B11 → B10 → B12. Every step ran the per-step gates
(RULE 16): pytest + node green, global gate fails never above 128,
coverage never below the previous step, vulture @90 = 0.

## Per-step outcomes

| Step | Commit | Deliverable | Outcome |
|---|---|---|---|
| B7 coverage completion (F-1) | `d9759ff` | real-behaviour tests for every owned module below 80/75 | preset_store 29.3→90.7, output_state 3.7→100, undo_store 76.0→96.9, site_adapter 81.8→100, config_manager 82.1→95.2, cooldown_store 82.8→93.3, json_store 86.5→100 (line%; branch all ≥80). Global 43.09/33.76 → 44.30/35.10; 459→504 tests |
| B8 vulture hygiene (F-2) | `93b9d31` | distinctive `*_shim_*` names + whitelist without bare `a`/`args` | probe proved the old whitelist silently masked `def prod_callback(*a, **kw)`; new whitelist flags it. vulture @90 = 0 |
| B9 RULE 21 hardening (F-3) | `b415a93` | byte-identical literal swaps + `tests/test_action_block_defaults.py` + DOM_SELECTORS §E/§F/§I | single_job_runner + 5 bridge fallback literals → probe_selectors accessors (values frozen by test); 23 lock tests (mirror families, frozen divergent defaults, JS `getDefaultBlocks()` mirror — which caught real pre-existing DOWNLOAD/SAVE name drift, source-scan for new literals); docs aligned |
| B11 stale docs (F-5) | `f8ae732` | superseded/historical banners + index rows + SOR §8 test rows | no doc outside archive claims deleted modules current |
| B10 output_probes dedup (F-4) | `cd8d14c` | characterization FIRST, then payload builders | key-set lock (after-reset variant), visualNext-rescue test, 96-geometry gate-invariant sweep proving the poolDetails mismatch return is a defensive unreachable branch (kept as download-path defense, RULE 10; construction locked); then `assocShape`/`mismatchReturn`/`jobRecord` extracted — field sets unchanged, 10/10 node scenarios green; jscpd 44→42 clones, output_probes internal 2→0 |
| B12 gate honesty (F-6) | this commit | `--base` + loud fallback warning, coverage ratchet, pre_push fix, subprocess tests | `verify_quality.py --changed --base <ref>`; no-merge-base now prints `⚠⚠ GATE HONESTY` to stderr + `changed_fallback` in JSON (was: silent all-files); `--coverage-ratchet` fails only on decrease vs baseline `coverage` key (absolute 80/75 warns as final D4 target); `--update-coverage-baseline`; `pre_push_check.sh` picks `.venv/bin/python`, generates fresh coverage before ONE authoritative gate pass — **exit 0 end-to-end at 44.31/35.10**; 6 subprocess tests |

## Final metrics (round B7–B12)

| Metric | Round start | Round end |
|---|---|---|
| pytest | 459 | 533 (+74) |
| node | 111 | 115 (+4) |
| Coverage line | 43.09% (5010/10999) | 44.31% (5143/11003) |
| Coverage branch | 33.76% (979/2900) | 35.10% (1018/2900) |
| Gate fails (absolute mode) | 128 | 130 (128 + 2 coverage lanes, now measurable because coverage.json is generated by the pre-push script; D4 closes them) |
| Gate fails (ratchet mode, pre-push lane) | n/a | 128 (coverage lanes warn) |
| jscpd | 44 clones / 473 dupLines | 42 clones / 454 dupLines |
| vulture @90 | 0 | 0 |
| `bash tools/pre_push_check.sh` | impossible (F-6) | **exit 0** |

## Findings resolution

- **F-1 (P0)** closed: all Area-B-owned modules ≥90% line / ≥80% branch.
- **F-2 (P0)** closed: whitelist hazard removed, proven by probe.
- **F-3 (P1)** closed for Area-B scope; CHECK_SECURITY/WAIT_OUTPUT selector
  semantics + JS/Python block-name drift documented and pinned
  (RULE 10 decision → Area A).
- **F-4 (P2)** closed: output_probes internal clones 2→0, payload field
  sets byte-locked; poolDetails branch documented as defensive-unreachable
  (kept).
- **F-5 (P1)** closed: banners + index + SOR §8 rows.
- **F-6 (P1)** closed: no silent fallbacks; ratchet; script green.
- **F-7 (minor)** closed via B7 (flatten_diagnostics directly tested).

## RULE 18 / RULE 16 end-check (per user instruction)

- Every function added or edited in app/ this round is ≤30 LOC (measured:
  max 25). All new tests are real-behaviour tests (RULE 8); no test
  inspects internals except the single documented source-shape lock for the
  unreachable defensive branch (justified in-test).
- Tools: my additions to `verify_quality.py` were split to fit
  (`check_coverage` 19 LOC, `metric_lane` 25 LOC); pre-existing
  `check_file`/`walk`/`main` sizes are legacy, unchanged, not grown beyond
  their baseline.
- §16.5 legacy hotspots: bridge.py fails 65 (unchanged), cdp_arena
  CDPArenaController 359 LOC / 28 methods (unchanged), CDPClient 456/21
  (signature-line edits only). Net LOC in touched legacy files: down.
- Anti-gaming: no `foo_part1` splits, no `**kwargs` dodges; the B8 rename
  made shim signatures MORE explicit, not less.
- Baseline `quality_baseline.json`: per-file keys untouched; only the new
  `coverage` key seeded (44.31/35.10) via the documented
  `--update-coverage-baseline` flag after Area B went green.

## Handover (unchanged from plan NOT-scope)

bridge/undo clone cluster → A5/A7; cdp_client API-surface @60 triage → C2;
preset_store method split → C6; global 80/75 → D4 (ratchet now enforces the
"never decrease" half of that lane); full per-metric ratchet → R0.3/D6.
