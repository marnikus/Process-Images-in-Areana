# Docs Map — Arena Image Processor

## Current truth (read these first)

These three files in `docs/current/` are the **single source of truth** for what the system does today. If not true today, it does not belong here (RULE 17).

| File | What it is | When to read |
|---|---|---|
| [`current/SYSTEM_OF_RECORD.md`](current/SYSTEM_OF_RECORD.md) | Authoritative spec: behaviour table (22 capabilities), state machine 00-22, core flow plan→execute, invariants, storage map JSON-only, key modules & layers, tests, quality gates, UI 15 windows, history pointers | Before any code change — understand what system actually does |
| [`current/AGENT_RULES.md`](current/AGENT_RULES.md) | Detailed code-quality rules (23 rules, adapted from Old App's 19 rules + 4 new for Arena). Includes RULE 1 visual runner, RULE 16 hard gates LOC 30/150 params 4 methods 15 CC 10 cognitive 15 nesting 4 coverage 80%/75%, override format, anti-gaming, remediation order RULE 19, selector priority, CAPTCHA compliance. Executable gate: `tools/verify_quality.py`, baseline `tools/quality_baseline.json`, pre-push hook `.git/hooks/pre-push` | Before adding/changing production code — mandatory gates |
| [`current/CODE_VERIFICATION.md`](current/CODE_VERIFICATION.md) | **Mandatory verification workflow before push** — how to run `tools/verify_quality.py --changed --allow-legacy`, `tools/pre_push_check.sh`, tests, coverage, git hook enforcement, override format, remediation order. Based on Old App's rule16_gate.py | Before every `git push` — RULE 16 enforcement |
| [`current/QUALITY_RECHECK.md`](current/QUALITY_RECHECK.md) | Latest RULE 16 gate snapshot (tests / JS / size-complexity / slot contract / coverage) with the debt carried — refreshed each release round | After a round, before push — compare your numbers |
| [`current/DOM_SELECTORS.md`](current/DOM_SELECTORS.md) | Living selector reference for arena.ai: verified selectors from saved HTML, primary+fallbacks, scope, visibility, expectedCount, verification, evidence, JS probes, visual runner colours, readiness composite, missing selectors list | Before touching `app/browser/site_adapter.py` or action blocks |

**Rule numbers are stable** — production code cites them (`RULE 1` … `RULE 23`). Never renumber; append instead.

---

## Supporting docs (still relevant, but not current truth)

These are kept for context, but `docs/current/` is authoritative if conflict:

| File | What it holds | Status |
|---|---|---|
| `data_model.md` | Data structures: ImageItem, URLItem, AppState, etc. | Supporting — should match SYSTEM_OF_RECORD §6 |
| `workflow_diagram.md` | Visual workflow of scan→select→attach→prompt→submit→wait→download→save | Supporting — state machine in SYSTEM_OF_RECORD §3 is authoritative |
| `implementation_plan.md` | Step-by-step build plan | Historical — implementation done |
| `selector_map.md` | Detailed selector map with evidence (181 lines) — source for DOM_SELECTORS.md | Supporting — DOM_SELECTORS.md is living reference |
| `research_summary.md` | Findings from saved HTML inspection | Historical research |
| `risks.md` | Risks, mitigations | Supporting |
| `known_limitations.md` | Known limitations | Supporting |
| `manual_test_checklist.md` | Manual testing steps | Supporting |
| `modern_ui_integration.md` | How sash-grid, win-grip, dark-mode variables.css reused from Old App | Supporting |
| `rules.md` | Old simplified rules (40 lines) — **deprecated**, use `current/AGENT_RULES.md` (730+ lines detailed) | Deprecated — pointer to current |
| `research/` | Saved HTML + assets from arena.ai | Evidence for selectors |

---

## Archive (dated, never edited to catch up)

`docs/archive/<YYYY-MM-DD>-<topic>/` — every design/plan/root-cause doc goes here, dated by day written. Archived docs are record of what was believed then, never edited.

- `archive/2026-09-16-image-below-prompt/` — fix for image-below-prompt correlation (JOB-ID verification for reverse layout)
- `archive/2026-09-16-test-time-reduction/TEST_TIME_REDUCTION_PLAN.md` — structured plan to reduce test time: pyramid rebalance, fixture scopes, eliminate real-browser tier, xdist parallelisation, async sleep removal, DI fakes, CI 3-stage gates, roadmap with RULE 16/18 compliance
- `archive/2026-09-17-watcher-grid-bugfixes/SOLUTION.md` — root causes + fixes for the blank Watcher window, "2nd divider rescales all rows" drag bug, and sashes vanishing after drop: single sash-visibility rule, hidden-child-safe resize commit, next-visible-child drag pair
- `archive/2026-09-17-titlebar-controls-visibility/SOLUTION.md` — window ─/✕ controls can be clipped when a window narrows (title row min-content > 96px floor, `.panel{overflow:hidden}`): title text truncated via `span.win-name`, secondary title items dropped right-to-left by single-writer `_fitTitleBars()`, fixed core trimmed below the 96px minimum
- `archive/2026-09-17-2captcha-integration/` — `design.md` + `summary.md`: opt-in 2Captcha auto-solve for visible Security-Verification captchas (RULE 20 amendment), `handle_captcha` choke point, masked-only key store, statistics panel, JS probes tested under `node --test`
- `archive/2026-09-18-captcha-detection-verification/design.md` — detection verified/adapted against real arena.ai states (challenge dialog vs always-on badge: two sitekeys, `size=invisible` vs `normal`): badge exclusion, dialog-scoped sitekey, `isInvisible` in the docs-exact 2Captcha payload, real container/text signals
- `archive/2026-09-18-captcha-delivery-recovery/design.md` — round 5: token never travelled the real solve path (anchor `cb=` premise disproved by live logs) — every-field injection + `data-callback` → `___grecaptcha_cfg` → anchor chain; bounded post-settle resubmit when the blocked generation died
- `archive/2026-09-18-revival-generalize/design.md` — round 6: trigger revival on spinner loss (seen, then 20 s gone with no pixels), not only on captcha settle — covers no-dialog dead generations like the 12:08 run
- `archive/2026-09-18-grid-window-set/design.md` — grid persistence: Python/JS window sets synced (14 incl. `captcha`); invalid layouts rejected not defaulted (RULE 13); preset save→load round-trips the portable doc
- `archive/2026-09-18-resubmit-send-ready/design.md` — round 7: visible-composer insert + Send-ready click + re-attach on resubmit; `Trace ID:` in-thread errors fast-fail the wait
- `archive/2026-09-18-solve-observability/design.md` — round 8: task id, poll heartbeats, token fingerprint + time-to-token, pre-inject dialog state, provider error detail in logs
- `archive/2026-09-18-too-late-to-solve/design.md` — round 9: tokens real-but-late verdict + data-first slice (isInvisible log, mid-solve error timestamps, no behavior change)
- `archive/2026-09-18-captcha-reporting/design.md` — round 10: `CAPTCHA_SOLVE` JSON per encounter + `CAPTCHA_JOB` join at job end (outcome + page error)
- `archive/2026-09-18-captcha-session-recording/design.md` — research/design for bounded redacted DOM/network recordings, CDP event routing, auto lifecycle, Records window, and user labels
- `archive/2026-09-18-captcha-solve-comparison-diagnostic/verification-and-problem-diagnostic.md` — evidence-gated manual-pass/bot-pass/bot-fail comparison protocol, confirmed recording gaps, root-cause hypotheses, and fix verification criteria
- `archive/2026-09-18-captcha-bot-failure-analysis/verification-and-problem-diagnostic.md` — why the bot solve fails, exactly: manual-vs-bot path structural diffs (D1–D6), re-verified recorder/viewer defects (P1–P10 with current-code evidence), confirmed failure mechanisms from rounds 5–11 live logs (RC-1…RC-5) with per-session recording verification signatures, and the ordered fix plan (F-A read-model fix + F1 all-records view + F2 per-row delete implemented; F-B…F-E proposed)
- `archive/2026-09-18-recaptcha-page-mechanics/research-design.md` — research/design: Enterprise bootstrap, anchor/challenge frames, response field, callback, backend acceptance, stale tokens, page-error state machine
- `archive/2026-09-18-recaptcha-verification-architecture/design.md` — implementation architecture: probe evidence, terminal page-error/stale-token guards, acceptance candidate vs output success, penalty/report semantics, test matrix
- `archive/2026-09-18-recaptcha-verification-architecture/implementation-2026-09-18.md` — implementation record: page/document timeOrigin identity + challenge-frame identity compared before token injection
- `archive/2026-09-18-url-row-tab-ownership/design.md` — "5 links detected but only 4 tabs open" + "checked 1 link but 2 were used": root causes were run-side row re-binding (round-robin `link_tab` steals rows → next scan adds phantom rows) and dispatch ignoring the checkbox (pool dispatch/tab-resolve picked any tab); fix is invariant I-33 — one live tab ↔ one row, auto-connect owns bindings, scan repairs duplicates, and checked rows gate start/single/parallel paths
- `archive/2026-09-18-code-quality-round/` — **planning round (no production code changed)**: `metrics-baseline-2026-09-18.md` (full audit vs the given thresholds: 1,047 py functions, worst `Bridge._do_run_batch` 1,205 LOC / CC 354 / cognitive 1,097 / nesting 23, Bridge 4,991 LOC / 200 methods / LCOM4 11 / MI 0.00, 120 RULE 16 fails, 1,239 LOC dead code, line 43.4% / branch 32.0%, 55 duplicate groups, JS un-gated), `problem-priority.md` (P1…P9), four isolated area plans — `plan-area-a-run-pipeline.md` (bridge/`_do_run_batch` ↔ `single_job_runner` unification + facade split), `plan-area-b-dead-code-purge.md` (controller/job_runner/output_detector/job_state_machine deletion + RULE 21 selector single-source), `plan-area-c-complexity-hotspots.md` (watcher/cdp_client/cdp_arena/… + JS panels), `plan-area-d-verification-and-evidence.md` (coverage 43%→80%, mutation, gate ratchet, captcha recording P9/F-B/F-C) — plus `appendix-reproduction-commands.md`
- `archive/2026-09-19-code-quality-implementation-plan/` — **implementation plan, planning only, no prod code**: `design.md` (process + area map + priority table), `step1-understand-prioritized.md` (Step 1 split into 10 sub-steps 1.1–1.10 by severity P1 96→P9 33, each with measure command/evidence/decision/owner), `area-plans-prioritized.md` (Round 0 R0.1–R0.6 + Area B B1–B6 first quick win, Area A A1–A7 centre of gravity, Area C C1–C8 + D D1–D7 parallel, all ≤20 LOC steps RULE 19 order nesting→CC→cognitive→size), `verification-checklist.md` (RULE 16 hard gates + RULE 18 ideals recheck template, per-step/per-area/final), `implementation-2026-09-19.md` (empty placeholder for future implementation record) — prioritized by blast radius×3 + evidence×2 + gate×2 + defect×2 + delay×1, implementation order Round0→B→A→C/D with D1 immediate
- `archive/2026-09-18-recording-toggle-and-day-folders/design.md` — record on/off toggle (default ON, `start()` short-circuits when off) + per-day session folders `YYYY-MM-DD/<session>/` (legacy flat coexists, `drop_empty_day`, analyzer walks all layouts)
- `archive/2026-09-19-rate-limit-penalty/design.md` — 3rd URL-window cooldown field: user-set **rate-limit penalty** (default 30 min) — when a job fails on a rate/limit/quota banner (`is_rate_limit_error`), the tab stacks that penalty (`maybe_note_rate_limit` at both failure sites) so it cools base+penalty instead of base alone
- `archive/2026-09-19-area-a-bridge-pipeline/` — **Area A implementation (centre of gravity)**: `design.md` (A1–A7: golden harness → converged runner → orchestrator seam → flip+delete → 12 panel mixins → BridgeContext → sweep) + `review-and-improvement-plan.md` (R0–R12 execution record: ideals hardening, 8 panel rounds with 87/87 + 70/70 + 39/39 behavior differentials, ideals sweep, context, sweep) + `implementation-area-a.md` (per-step log with SHAs, packing table, coverage baseline, F17 follow-up). End state: 590 green, gate TOTAL 62 (bridge 0), Bridge 159 lines / 10 methods, 119 frozen slots, panels→services direction + one Qt shim
- `archive/2026-09-19-area-d-implementation/` — **Area D (verification & evidence)**: `design.md` (D1–D7: `dropped_events`, bounded token-free milestones, actor≠result labels + cohort, coverage ramp, mutation, gate hole) and `d5-mutation.md` (scoped mutmut tooling, survivor triage classes A/B/C/D, the `session_id` `..` path bug it found, plus per-scope results under `d5-results/` — `branch-d/` holds D's original numbers, the top level the re-run on the integrated tree)
- `archive/2026-10-02-captcha-watcher-isolation/` — **Captcha Watcher isolation + 5 UI bugfixes**: `audit.md` (who solved captchas before: ~4,100 LOC chain inventory; UI root causes B1–B5), `design.md` (contract: pipeline never solves, `app/services/captcha_watcher/` SDK-only loop is the only solver, runs only while the Watcher is ON; 6 solver slots + `restore_default_blocks`; kept-on-purpose deviations — verified probes, wait-only `handle_captcha`, recordings, recovery — and follow-ups F-1…F-3), `bugfix-verification.md` (boot helpers `js/core/boot.js`, URL-list listeners + `commit_urls`, action-blocks empty-stack healing, preset id collisions → static markup, folder-shape normalisation — each pinned by a named test)
- `archive/2026-09-21-reparse-sweep-and-checkbox-pool/design.md` — **manual Reparse = fresh sweep + the URL checkbox owns pool membership (I-56)**: D-1 interval parameter mirrored into the Settings panel (one key `url_reconcile_interval_ms`, one `save_settings` funnel — RULE 10), D-2 fetch-first sweep (busy rows keep identity, checkbox memory restores checks, failed fetch never removes — I-50), D-3/D-4 one decision `url_policy.pool_exits`, two enforcers (`commit_urls` instant + reconciler pass via `LiveDeps.leave_tab`), D-5 `plan_auto_connect` rejoin gate (re-checked rows rejoin on the next pass, persisted cooldown restored)
- `archive/2026-09-21-reparse-rejoin-and-pool-tab-id/design.md` — **Reparse keeps its rows, re-check rejoins at once, one tab id** (bugfix round: D-1 the pass commits the rebuilt rows even when the pool phase fails + a failed save is retried next pass, D-2 `reconcile_loop` survives a raising pass, D-3 the checkbox gate's join half acts on the commit — `enter_pool_for_checked` → `rejoin_checked_rows` — D-4 the pool table shows the badge's full tab id (no 12-char slice; the snapshot reports the pool key), D-5 an empty manual rebuild names the pattern)
- `archive/2026-09-23-uivision-tab-match/design.md` — **"No tab match": the app was reading a store that does not exist (I-66)** (user: the macro "reports 'no tab match'" although Firefox had the Arena tab open and the app's own log showed it added). Not a Ui.Vision problem at all — two defects in the I-65 lookup, both the same mistake of coding against an imagined interface. `_urls_of` read `bridge._url_rows()` / `bridge.url_rows`, neither of which exists in the codebase; the rows live in `bridge.state.urls`, so the pattern was always matched against an empty list. Beneath it, `matching_urls` fell back to `str(row)`, and a `UrlRow` repr *contains* its url — so a row "matched" through its repr and the repr was handed back as if it were a URL, meaning a fix to the first defect alone would have launched Firefox at a garbage locator. One accessor `url_of()` now handles the three real shapes with no `str()` fallback. The I-65 tests missed both because they used dict rows the app never produces. The failure was also unactionable — "no tab matches the pattern" looks the same whether the pattern is wrong or the app is reading nothing — so `match_report()` now names the pattern, the row count and the URLs it compared. pytest 2,141 · JS 347 · coverage 88.70/85.32 · gate 0 fails
- `archive/2026-09-22-firefox-uivision-extension/design.md` — **Firefox auto with Extension — the debugger channel deleted, Ui.Vision RPA in its place (I-65)** (user: "Firefox stays a normal browser … XClick sends native OS input, isTrusted:true … no geckodriver/Selenium/Playwright/Puppeteer"): I-62…I-64 drove Firefox over its DevTools RDP, which needed `--start-debugger-server` — and Firefox authorises **every** incoming debugger connection, so even after I-64 cut it to one dialog per browser the port itself was still the automation surface, plus a second protocol to maintain beside Chrome's CDP. The whole channel is gone (26 files) and Chrome is back to exactly what it was. Firefox is now an ordinary browser launched with **no automation flags at all** — the ban is data (`LAUNCH_BANNED_FLAGS`) and `launch_argv` raises on any of them, so the guarantee cannot erode. The work happens in the signed Ui.Vision extension, whose Command Line API turns out to be a **URL, not a CLI**: `file:///…/ui.vision.html?macro=…&direct=1&closeRPA=0&savelog=<abs>`. Three encodings are load-bearing — the `file:///` form (a bare `C:\…` makes the browser swallow the query as a filename), percent-encoding (the XClick target is full of `/ [ ] ' =`), and `closeRPA=0` against a **default of 1** that would tear the log down mid-poll. `Click` is refused at build time because a DOM click is silently wrong (`isTrusted:false`); `XClick` moves the real OS cursor through XModules. Completion has no callback — Python polls the macro's `savelog`, and "no verdict yet" means still running, never failed. New 18th window exposes the **tab pattern as a control**. pytest 2,133 · JS 345 · coverage 89.45/85.31 · gate 0 fails
- `archive/2026-09-22-firefox-connection-prompt/design.md` — **One connection per Firefox, not one per scan (I-64)** (user: "stop this popup dialog win to generate every sec!"): Firefox authorises **every incoming debugger connection**, so the app's connection count *is* the user's dialog count — and `list_firefox_tabs` attached and detached on every call while the reconciler ran it on a 5 s (down to 500 ms) timer. The tidy `finally: detach()` was the bug: against a protocol that prompts per connection, reconnecting politely became a popup storm. A shared session cache now holds one live connection per browser — **20 scans open 1 connection, not 20** — serialised so concurrent scans cannot each open one, with a single retry when a link genuinely dies. That leaves one dialog per Firefox start; the app now also prints how to remove it for good (`devtools.debugger.prompt-connection=false`, plus a paste-ready `user.js`), once per endpoint rather than once per scan. pytest 2,152 · JS 291 · coverage 88.83/85.39 · gate 0 fails
- `archive/2026-09-22-multi-browser-port-routing/design.md` — **One app, several browsers, one protocol each (I-63)** (user: "i can not connect the Firefox browser… every browser has it individual port"): the app asked Firefox for `http://localhost:9224/json/list`, a **Chrome-only** endpoint, got `Not Found`, retried forever and logged it as a "Chrome connection error". Firefox on `--start-debugger-server` serves no HTTP and no WebSocket — it speaks raw-socket RDP — and the single `cdp_host`/`cdp_port` setting had nowhere to record that 9224 is a different browser, so the (correct) I-62 RDP client was never called. Now `browser_endpoints` declares a kind per port (`browser_base_port` derives 9223/9224/9225), `browser_scan` routes each endpoint to its own protocol and scans them concurrently so one dead browser cannot hide a working one, Firefox tabs carry an opaque `rdp://` locator that routes every downstream fork (`FirefoxPoolClient`, pool key, no CDP controller for a click-only tab), and failures name the real browser plus the exact flag to start it with. Legacy single-Chrome installs are untouched. 73 tests, mutation-checked. pytest 2,143 · JS 291 · coverage 88.77/85.34 · gate 0 fails
- `archive/2026-09-22-firefox-rdp-stealth-attach/design.md` — **Firefox is attached, never driven (I-62)** (user: the previous Firefox integration "is not working as expecting"): reimplemented on the one remoting channel that keeps `navigator.webdriver === false`. The property has a single implementation (`dom/base/Navigator.cpp`, `Navigator::Webdriver`) reading exactly two sources — Marionette and the Remote Agent — so `--marionette` (geckodriver/Selenium) and `--remote-debugging-port` (BiDi/CDP) both expose automation, while the DevTools server (`--start-debugger-server`) is a third mechanism the property never consults. New leaf package `app/browser/rdp/` (6 files, 589 LOC, **zero** new dependencies) speaks the length-prefixed RDP (`<byte-length>:<json>`) over a plain socket: the user launches their own GUI profile, the app only attaches, and detach leaves the session running. Load-bearing protocol facts, each a named function with a test: the prefix counts **bytes**, a reply is told from an event by **shape** (no `type` key) never by sender, a timeout **poisons** the link, `evaluateJSAsync`'s ack is not its result, and actor ids are **never** cached across a connection. 65 tests against real sockets; residual: the local "reason: DevTools" chrome hint (not web-observable). pytest 2,072 · coverage 88.54/85.06 · gate 0 fails
- `archive/2026-09-21-free-worker-dispatch-gate/design.md` — **A free `steady (ready)` worker must not read as "no usable tab" (I-61)** (user report: "6 pending i web page ready. but it wait and not run the job but should use free web page with status ready!"): the live pass gate asked only whether the *primary CDP tab* resolved, and `resolve_and_claim_tab` answers `''` when nothing is attached and the reconnect fails — so a pool holding one `waiting_generation` worker and one idle `steady (ready)` worker parked the run on the `no tab` wait state while 6 images waited. The parallel feeder never uses that connection (each job runs through its pooled page's own client), so `plan_pass` now also opens on `_parallel_ready` — the same `counts_in(pool, allowed) >= (2, 1)` predicate `_try_parallel` uses to pick the lane. Single-tab `no tab` and `all cooling` keep their meaning; an uncountable pool waits (RULE 4). RED-first integration test `tests/integration/test_free_tab_dispatch.py` (4). pytest 2,007 · JS 334 · coverage 88.43/85.02 · gate 0 fails
- `archive/2026-09-21-stop-cancel-release-pipeline/design.md` — **Stop & Cancel actually release the tab (I-60)** (user report: status stays unchecked, the row keeps showing the image, Stop does nothing, no cooldown elapses): the post-job cleanup lived *inside* the job task, so cancelling the future skipped it and the Stop flag was never read by a parked `await`. New owner `services/tab_release.py` runs the release as the action's own step — abort flag cleared, `current_image` forgotten, waiting overlay hidden, `reset_to_new_chat` **without** a cancel hook (honouring the cancel flag there is what made the existing mechanic a no-op), base pause + operator penalty armed, pool re-emitted — with Cancel sweeping every interrupted tab from a synchronous snapshot, an idle tab never penalised (RULE 4), and Clear Time confirming visibly. pytest 1,999 · JS 334 · coverage 88.39/84.96 · gate 0 fails
- `archive/2026-09-21-job-history-merge/design.md` — **Job History window merged onto the readable-tab-ids branch**: the 17th window (`job_history`, `GRID_VERSION` 6 → 7) cherry-picked from `arena/01a0c3a5-…`; the `package.json` `test:js` list resolved as a union (both branches' new suites run), and the one semantic conflict fixed — the window's "Tab ID" column predated D-7, so it now prints the readable `{email}_{4 digits}` handle through the existing owners (`job_history.tab_label_for` → `page_pool.tab_label_of`; JS `TabLabel`), frozen into the row at record time so a finished job keeps its name after the tab closes (full hex stays in the tooltip, `tab_id` unchanged as identity). pytest 1,963 · JS 328 · `job_history.py` 100 % line+branch
- `archive/2026-09-21-pool-follows-url-rows/design.md` — **the pool never holds a worker no URL row owns (I-58)** (user report "2 url only but the pool have 4 worker … never more"): `url_policy.pool_exits` now reports both halves of the one membership rule — `unchecked` (I-56) and `orphan` (no row owns the pooled tab) — with one shared wording and the same two enforcers (`url_queue.enforce_pool_membership` on every row commit, `reconcile._enforce_membership` every pass); ownership rather than liveness decides, busy tabs defer (RULE 15). Fixes the zombie-worker accumulation that also fed the `Connect failed … HTTP 500` primary-connect churn
- `archive/2026-10-09-run-scope-and-config-hygiene/design.md` — **B13 API keys on GitHub + `completed` images sent again**: A (runtime `config/`, `logs/`, saved pages, bytecode out of the index; hygiene test + pre-push step 0; I-43), B (`app/core/run_scope.py` — the one run-scope predicate, re-checked at claim time in both loops, I-44; Start refused while a batch future is alive, I-45), C (resume on scan — a newly added source with an existing `_AI` sibling enters as `completed`, I-46); the doc records the audit, the plan the owner approved, the outcome, the same-day validation pass against all 23 rules (V1–V6: one predicate at Start and claim, one `_AI` vocabulary in `naming`, RULE 4 empty scan, `core/progress.py` extraction) and the RULE 16 / RULE 18 recheck table. Evidence per test: `archive/2026-10-02-captcha-watcher-isolation/bugfix-verification.md` §B13
- `archive/2026-09-20-live-processing-and-watcher-scope/` — **implementation plan (round 1) — integrated 2026-09-20 through the staged chain S0…S10 below; kept verbatim (RULE 17)**: `evidence.md` (what the code does today for the four requested changes, every claim with `file:line`, plus the frozen contracts — 134-slot surface, per-file ratchet, JS `file_lines` ratchet, coverage floors, goldens, pinned log strings), `design.md` (contract, decisions D-1…D-10 with rejected alternatives, new `app/services/live/` package + `app/services/captcha/policy.py`, per-feature design, 5-stage sequencing, new invariants I-39…I-42, risks, out-of-scope), `quality-budget.md` (RULE 16/18 numbers: per-file ratchet headroom, per-new-symbol LOC/CC/params budget with the test that kills it, test matrix incl. every existing test that must change, gate commands, RULE 16.7 checklist, anti-gaming refusals). Scope: 01 captcha work only while the Watcher switch is ON (RULE 20 / I-19 / I-34 amendment), 02 Python-owned passive URL reconciler that adds/removes rows in any run state, 03 always-live run loop that ends only on an explicit stop, 04 one queue read model + one write funnel that wakes the loop *(plan only — see the merge note under the round-2 entry below before S1: invariant numbers and the eligibility rule moved under it)*
- `archive/2026-09-21-run-cycle-badge-and-instant-dispatch/design.md` — **run-cycle ON/OFF badge + idle-worker instant start — LANDED 2026-09-21** (A-1…A-4 the badge as a view of the one `run_state` with a sub-label beside it; B-1…B-4 the root cause — a frozen per-pass image list behind `asyncio.gather` — and the feeder that replaces it, bus-woken free-page wait, the `len(images) >= 2` gate removed; I-57 — renumbered from I-56 at the merge with `arena/01a0c355-…`, whose checkbox-owns-pool invariant keeps I-56).
- `archive/2026-09-20-run-badges-and-worker-overlay/design.md` — **run-status badges + worker-id overlay — LANDED 2026-09-20** (D-1…D-6: the badge is a view of the one `run_state`, one JS painter for every `[data-run-badge]`, `worker_no` assigned once by `PagePool.add_page`, badge JS in its own leaf `browser/worker_badge.py`, cadence = join + every reconciler pass, 0 new slots/signals; §5 the RULE 16 / 18 recheck).
- `archive/2026-09-20-dynamic-urls-and-worker-debug/` — **round-2 implementation plan — INTEGRATED 2026-09-20: S0…S10 all landed on `arena/01a0bfba-…` (`57cfae1` … S10), see `current/QUALITY_RECHECK.md` for the per-stage numbers and the S10 consolidation; plan kept verbatim (RULE 17)** (amends round-1 D-1/D-6/D-9; both rounds are delivered as **one staged chain S0+S1…S10** — rev 2 reversed the single wave at the owner's instruction: separable features, most important first, each stage shippable and gate-green on its own): `evidence.md` (§8 = the dependency facts that fix the stage order; the URL cadence is a JS 15 s timer + a hard-coded 5 s plan value; the generation timeout **cannot** survive a captcha wait — `LoopState.start` vs the unbounded `wait_captcha_cleared`, with the full chain; reset semantics park the queue; L-1 re-verified; the window-registry chain end-to-end; **new defect L-5** — `index.html:263` `data-window="page_pool"` was never registered, so `SashGrid.render()`'s `replaceChildren` destroys the Page Pool panel and its worker table on first paint; the Python/JS ratchet asymmetry — Python `file_lines` is recorded but not enforced, JS `file_lines`+`func_count` growth fails for **every** baselined file — plus a verified per-file headroom table), `design.md` (contract per item **with a Stage column**, decisions **D-11…D-27** with rejected alternatives (rev 3 adds **D-25** `PauseClock` ⇒ `core/pause_clock.py`, **D-26** the two ratchet-forced splits — `handle_captcha` because CC 7 is the file maximum, and `_settle_timed`/`_timeout_text` because `cdp_arena/output.py` sits at CC 4 / nest 1 / params 4 — and **D-27** the test-integrity rule that closes latent defects **L-6** (a test doubled the seam it tests), **L-7** (two `.mjs` tests are on disk but unlisted, so they never run) and **L-8** (the dead, drifted `WIN_ICONS` table is deleted, not extended)) — **D-12R** moves the interval control into the URL List job-cycle bar and **D-24** is the staging rule itself, architecture deltas — `core/window_catalog.py`, `services/live/debug_view.py`, `PauseClock` in its own **`core/pause_clock.py`** (rev-3 **D-25**: `output_wait.py`'s `max_class_loc` is **4**, so no new class can live there and the clock rides `WaitSpec.pause`), 4 new `live-debug` JS files + CSS; item 01 `url_reconcile_interval_ms` 500…60000 default 5000, written through the existing `save_settings` slot from a **new** `url-list/interval.js` control because `settings.js` and every `url-list/*.js` file are frozen (D-12R — the debug window shows cadence read-only); item 02 `PauseClock` ⇒ `elapsed = now − start − paused`, **capped** at the existing `watcher_captcha_timeout_sec` (owner correction — the wait ends at the cap and the job fails honestly as `wait_timeout`; worst case per image `generation_timeout + captcha_cap`), Watcher OFF = **no captcha activity at all** and no pause because neither settler nor cap is installed, no-key wording fixed; item 03 receiver flag + ⊘ icon with **zero JS line growth** and **D-6 reversed** so every reset re-queues; item 04 L-1; item 05 the 16th window **Live Worker & Queue Debug** rescuing the destroyed pool panel, riding `page_pool_updated`/`progress_updated`/`arena_state_updated` with **no new slot and no new signal**, self-connecting so `listeners.js` stays untouched; §9 the **stage chain S0 prerequisite + S1…S10** with per-stage files, tests and doc rows (§9.3), the un-splittable edit groups (§9.4) and each stage's standalone value (§9.5); invariants **I-43…I-46** tagged by stage; risks R11…R26 incl. the two staging traps; out of scope), `quality-budget.md` (gate commands + bootstrap, global floors 86.09/82.01/1.240 %, per-file ratchet budget with symbol spans and coverage floors, the **JS net-zero ledger** (0 new lines/funcs in any baselined `.js`, −1 in `cdp.js`), new-file budgets with the test that kills each, RULE 18 recheck table, test matrix incl. every existing test that must change (`test_grid_layout.py` 15→16 ×4, `sash_harness.mjs` lists, `package.json` test list) and the ones that must stay **unedited** (`test_bridge_slots.py`, `test_bridge_metaobject.py`, `test_cooldown_service.py:604-618`), RULE 17 doc rows mapped per stage, RULE 16.7 checklist **per stage (§8.0) + final (§8.1)**, anti-gaming refusals incl. "no stage inside another stage's files"), **`tdd-interfaces.md`** (rev 3 — the owner's "pre-design the interface splitting, the expected files/functions and the branch of tests, TDD first": §0 how to use it + the TDD rules + the test-infra inventory + L-6/L-7/L-8; §S1…§S10 give, **per stage**, the interface split (every new/changed symbol with signature, measured size budget, caller and a *must-not* column), the expected files and functions, the **RED-first test branch** (134 new test functions across 19 new test files, each with the failure expected at base) and GREEN + REFACTOR + equivalence + gate + docs; §A the frozen seams and the test that pins each, §B the proof that the chain needs **0 new slots and 0 new signals**, §C the test ledger with measured base counts (116 `.py` test files, 25 listed `.mjs` of 30 on disk), §D the **measured headroom table** for all 25 touched files (loc/CC/nest/params per symbol, re-measured with the gate's own `current_maxima()` — **0 breaches at `6bbaf8b`**, two *unenforced* drifts: `single_job_runner.py` 902→939, `undo_entries.py` 404→406), §E the anti-gaming rules for this chain). Scope: 01 URL update interval setting (ms), 02 captcha ⇄ generation timeout (pause per page while Watcher ON, **capped** at `watcher_captcha_timeout_sec`, zero captcha activity while OFF, manual-solve wait without a key), 03 URL rows (never-linked rows kept, "not used as job receiver" icon, all reset images re-queued), 04 L-1 `_schedule_coro` → `schedule_coro`, 05 Live Worker & Queue Debug window (workers + job status, pending count + first image name, real time, new/removed webpages reflected), 06 **staged delivery order** (S1 L-1 → S2 captcha scope → S3 capped pause → S4 live queue + reset re-queue → S5 always-live run → S6 dynamic URLs + interval → S7 receiver icon → S8 16th window → S9 debug content → S10 consolidation) **Merge note (read first): `archive/2026-09-20-dynamic-urls-and-worker-debug/merge-note.md`** — both plan folders were written against `6bbaf8b` (B9) and merged into this branch after B10–B13 landed; the plan's invariant numbers I-39…I-46 are already taken (land the plan's as I-47…I-54), the one eligibility rule already exists (`app/core/run_scope.py`, I-44 — S4 extends it, no second rule), the slot surface is 135 not 134, the coverage floors are 86.36/82.33, and the §D headroom table must be re-measured for the 10 files B10–B13 touched. L-1/L-5/L-6/L-7/L-8 are all still open.
- `archive/2026-09-19-refactor-cycle-integration/design.md` — **cycle X**: how R0+A+B+C were merged into one branch (per-deliverable winners with evidence), the three regressions the integration caught (silent `@Slot` swap, `CDPClient.connect()` TypeError in real Qt, RULE 21 literal drift), the payload-equivalence check, and the RULE 18 audit of what is still >300 LOC

Legacy study tree: the Old App (`Process Images in Areana/Old App/**`, 948 files) was **removed in R1** — it had already given what it had (UI system, action-blocks idea, and the detailed rules now living in `current/AGENT_RULES.md`). It remains readable from history at commit `11e6520`; see `archive/2026-09-19-refactor-cycle-integration/design.md` §7.

---

## How to add new doc

1. Write design into `docs/archive/<YYYY-MM-DD>-<topic>/design.md` (dated)
2. Update rows in `docs/current/SYSTEM_OF_RECORD.md` it affects (behaviour table, invariants, flows, history links)
3. Update `docs/current/AGENT_RULES.md` if new rule, or `DOM_SELECTORS.md` if new selector
4. Update this map

Do not add new top-level doc for feature — pointer outward beats wall of prose (RULE 17, RULE 18 context files 60-200 lines ideal).

---

## Quick links for agent workflow (RULE 16 §16.6)

1. Read `current/SYSTEM_OF_RECORD.md` + `current/AGENT_RULES.md` rules 1-15 + RULE 18 size ideals
2. Research saved HTML in `research/` + `selector_map.md` + `current/DOM_SELECTORS.md`
3. Design in `archive/<date>-<topic>/` if complexity moves across files — record radon numbers, target numbers, dishonest reductions rejected
4. Tests first (RULE 8)
5. Measure: `radon cc -s path/to/file.py` — any new function C or worse (CC≥11) → stop and redesign in order RULE 19
6. Update current docs in same change (RULE 17)

---

*Last updated: 2026-09-23 — **"No tab match" fixed: the app was reading a URL store that does not exist** (I-66, design `archive/2026-09-23-uivision-tab-match/design.md`): the new Firefox window always reported "no tab matches the pattern", even with the Arena tab open and listed in the app. The cause was not Ui.Vision but two defects in last change's lookup — it read `bridge.url_rows`, an attribute that exists nowhere (the real store is `bridge.state.urls`), and underneath that a `str(row)` fallback would have matched a `UrlRow` through its *repr* and returned that repr as the URL. Both are fixed behind one accessor, tested against the app's real row type and mutation-checked. A failed match now explains itself — which pattern, how many URLs, and which ones — instead of a bare "no match". Earlier — **Firefox auto with Extension** (I-65, design `archive/2026-09-22-firefox-uivision-extension/design.md`): the Firefox debugger channel built in I-62…I-64 is deleted outright and Chrome is left exactly as it was. Firefox is no longer remote-controlled at all — it is launched as a normal browser with no automation flags, and a signed Ui.Vision RPA extension drives it with **native OS mouse and keyboard input** (`XClick`, `isTrusted:true`), so there is no debugger port to authorise and no driver to detect. Python starts a macro by opening the extension's autorun URL in a new tab and learns the result by polling the macro's log file. A new 18th window, "Firefox auto with Extension", holds the configuration — including the **URL pattern** that picks which tab the macro opens — and runs the XClick demo against Arena's "New Chat" button. This iteration is the framework plus one proven action, as scoped; Chrome-parity functionality follows. Requires the Ui.Vision add-on and XModules, with Firefox visible and in the foreground. pytest 2,133 · JS 345 · coverage 89.45/85.31 · quality gate green. Earlier — **One connection per Firefox, not one per scan** (I-64, design `archive/2026-09-22-firefox-connection-prompt/design.md`): Firefox prompts for every incoming debugger connection, and the tab scan opened a fresh one on every reconciler pass, so the "Incoming Connection" dialog reappeared every few seconds. One shared session per browser now serves every scan (20 passes → 1 connection), and the app explains the one pref that removes the dialog entirely. Earlier — **One app, several browsers, one protocol each** (I-63, design `archive/2026-09-22-multi-browser-port-routing/design.md`): Firefox would not connect because every tab fetch went to `/json/list`, which only Chrome serves — Firefox on `--start-debugger-server 9224` speaks raw-socket RDP — and the single-endpoint setting had nowhere to say which browser lives on which port, so the RDP client added in I-62 was never reached. Ports are now declared with a protocol each, scanned concurrently, routed by an `rdp://` locator, and every failure names the browser it came from. Legacy Chrome-only setups behave exactly as before. pytest 2,143 · JS 291 · coverage 88.77/85.34 · quality gate green. Earlier — **Firefox is attached, never driven** (I-62, design `archive/2026-09-22-firefox-rdp-stealth-attach/design.md`): the Firefox connection was reimplemented as a stealth attach. `navigator.webdriver` is true iff Marionette or the Remote Agent runs, so every driver-shaped channel is excluded by construction and the DevTools server (`--start-debugger-server`) — which the property never consults — is the only viable one. The user launches a normal GUI Firefox on their real profile; the new leaf package `app/browser/rdp/` attaches over a plain socket with zero new dependencies, clicks via the console actor, and detaches without disturbing the browser (actor ids are re-enumerated every reconnect, never cached). 65 tests run against a real socket speaking the real protocol, mutation-checked. pytest 2,072 · coverage 88.54/85.06 · quality gate green. Earlier — **A free worker is work** (I-61, design `archive/2026-09-21-free-worker-dispatch-gate/design.md`): a run with 6 pending images sat on `RUN CYCLE ON · no usable tab` while one pooled worker showed `steady (ready)` — the pass gate only asked whether the single primary CDP tab resolved, although the lane that would run the work (the parallel feeder) drives every job through the pooled page's own client. `plan_pass` now opens whenever the feeder can run, reusing `_try_parallel`'s own `counts_in(pool, allowed) >= (2, 1)` predicate so gate and lane can never disagree; the real wait states (single tab without a primary connection, all cooling, uncountable pool) still say so loudly. RED-first `tests/integration/test_free_tab_dispatch.py`. pytest 2,007 · JS 334 · coverage 88.43/85.02 · quality gate green. Earlier — **Stop & Cancel release the tab** (I-60, design `archive/2026-09-21-stop-cancel-release-pipeline/design.md`): killing the run is no longer confused with freeing the worker — one pipeline (`services/tab_release.py`) returns the page to a new chat, drops the waiting overlay, clears the processing image and arms the pause plus a configurable operator penalty, for the Stop button and for every tab a cancelled cycle interrupted; Clear Time now confirms itself with an instant `00:00` and a colour flash. pytest 1,999 · JS 334 · coverage 88.39/84.96 · quality gate green. Earlier — **Job History window merged in** (`arena/01a0c3a5-…` → this branch, design `archive/2026-09-21-job-history-merge/design.md`): the 17th window logs every finished job newest-first (readable tab handle, worker `#n`, job number, ✓/✗ + error, image, folder, output link, times, 🛡 captcha count; display count 5–500 mirrored in Settings, last 500 kept in `config/job_history.json`). The merge reconciled it with D-7 so history and the worker table can never print different handles for the same tab. pytest 1,963 · JS 328 · quality gate green. Earlier — **readable tab ids + a countdown that never hides** (I-59, `arena/01a0c40f-…`, design `archive/2026-09-21-readable-tab-ids-and-live-countdown/design.md`): every tab is named `{email}_{4 digits}` — the number allocated once at the join and persisted with the timers (`config/cooldowns.json.aliases`), the account read from the sidebar via the RULE 21 `account_email` selector, labelled through one source (`PageInfo.label` → `tab_label_of` → JS `TabLabel.of`) in the worker table, the new URL-list `Tab` column, Live Debug, the in-page badge and the tab-lifecycle log lines — and the cooldown cell always shows the ticking `MM:SS` from `cooldown_until` whatever the status label says (`00:00` when ready, `—` only for "no pooled tab"): a captcha/rate-limit penalty on a resting tab now cools it at once, a busy tab carries a named `+15:00 debt` that becomes the live timer when its job ends (Cancel included). TDD from RED (`tests/test_cooldown_timer_visible.py`, `test_tab_alias.py`, `test_tab_owner.py`, `test_page_pool_alias.py`, `test_restart_identity.py`, `test_pool_join_identity.py`, `tests/js/test_countdown_visible.mjs`, `test_tab_label_views.mjs`); pytest 1,936 · JS 309 · coverage 88.09 / 84.73 · no frozen file grew (new `url-list/cells.js`, `page-pool/cells.js`, `page-pool/ticker.js`, `core/tab-label.js`). Earlier — **run-cycle ON/OFF badge + idle-worker instant start** (`arena/01a0bfba-…`, design `archive/2026-09-21-run-cycle-badge-and-instant-dispatch/design.md`): (A) the badge is now two persistent states — **RUN CYCLE ON** for everything but `idle` (paused / cooldown / captcha stay ON) and **RUN CYCLE OFF** only after an explicit stop — never hidden, in both the Run Controls title and the Live Debug head, with a sub-label *beside* it naming paused / stopping / the live wait reason (`live.wait_reason`, published from the supervisor's `_live_reason`; 0 slots, 0 signals); (B) **bug fixed, I-57**: `dispatch_parallel` froze the pass's image list and gathered one task per image, so an image queued mid-pass waited for the whole pass while a ready tab idled — it is now a feeder that re-reads the one eligibility rule while the pass is open, claims the page before creating the task, waits on the bus (woken by `page free`) and runs for any image count on 2+ pages; `live/__init__` stops re-exporting `supervisor` (import cycle). RED-first `tests/test_instant_dispatch.py` (6), `test_live_debug_view.py` (+1), `tests/js/test_run_badge.mjs` (6). Dispatcher file max LOC 29 → 18, cog 8 → 5. Goldens byte-identical. Earlier — **run-status badges + worker-id overlay landed** (`arena/01a0bfba-…`, design `archive/2026-09-20-run-badges-and-worker-overlay/design.md`): the run cycle shows a loud **PAUSED / STOPPING / STOPPED** badge (hidden while running) in both the Run Controls title and the Live Debug head — one painter `js/core/run-badge.js` off the existing `progress_updated.run_state`, no new state/slot/signal; every pooled Chrome tab carries a top-middle **`#n` + full tab id** badge (new leaf `browser/worker_badge.py` + `services/live/worker_badges.py`, re-asserted on every join and reconciler pass, cleared on leave), and the number — pool join order, session-stable, never reused (`PagePool.add_page` → `PageInfo.worker_no`, I-55) — leads the pool table's Tab ID cell and each Live Debug worker line. RED-first `tests/test_worker_numbering.py` (5), `tests/test_worker_badge.py` (10), `tests/js/test_run_badge.mjs` (5); pytest 1,790 · JS 280 · coverage 87.89 / 84.54 · jscpd 1.072 %. Earlier the same day — **code-quality review of S0…S10** (`current/metrics_report_2026-09-20-s0-s10-chain.md`): every RULE 16/18/19 metric measured with radon 6.0.1 / cognitive-complexity 1.3.0 / vulture 2.16 / coverage / jscpd / `js_metrics.js` on the final tree — 0 fail-line breaches, 0 new smells, one §16.3 gap found and closed in the same commit (`supervisor._crash_tail` had zero hits → `test_a_crashing_pass_ends_the_loop_loudly_and_idles`, deletion-checked; pytest 1,773). Earlier the same day — **S10 landed — the S0…S10 chain is complete** (`arena/01a0bfba-…`): consolidation only — the three RED checks pass on the final tree (every I-47…I-54 enforcement file exists and every stage commit updated `docs/current/`; the frozen-JS net-zero guards hold; the L-7 orphans run in `test:js`), the whole gate `tools/pre_push_check.sh` is green except the two sandbox `libGL` coverage floors that predate S0, `tools/quality_baseline.json` deliberately **not** re-recorded (reasons in `current/QUALITY_RECHECK.md` §S10), SYSTEM_OF_RECORD §11 now lists 16 windows, and both 2026-09-20 plan folders are marked integrated. Final numbers: pytest 1,772 · JS 275 · coverage 87.82 / 84.48 · jscpd 1.078 % · 135 slots. Previously — **S9 landed** (`arena/01a0bfba-…`): the Live Worker & Queue Debug window has its content — `live/debug_view.live_view` (`queued` / `next_image` from the one eligibility rule, `receivers` from S7's flag, `run_state`, `captcha_cap_sec` only while the Watcher is ON) rides `progress_updated.live` by a same-line swap in `emit_arena_state`; four new JS files (`panels/live-debug.js` + `live-debug/{store,render,actions}.js`) render the queue-head strip, one job-centric line per worker tab (`waiting_captcha` shows `timeout paused MM:SS (cap MM:SS)`), receiver counters and the read-only cadence line from the two pushed payloads with a local 1 s ticker — no slot, no signal, no poll, `listeners.js` frozen; RED-first `tests/test_live_debug_view.py` (7) + 6 JS tests; pytest 1,772 · JS 275 · coverage 87.82 / 84.48 · jscpd 1.079 %. Next: S10 (consolidation). Previously — **S8 landed** (`arena/01a0bfba-…`): the window contract is one table — new `app/core/window_catalog.py` (`WINDOWS` 16 ordered, `WINDOW_IDS` / `WINDOW_TITLES` derived, `GRID_VERSION` 6; `layout_service` 300 → 258 lines re-exports it), the 16th window **`live_debug`** ("Live Worker & Queue Debug", `#winLiveDebug`, `LiveDebugPanel` shell + `live-debug.css`) registered in Python / `constants.js` / `store.js` / `_PANEL_INITS` / `index.html` by same-line appends; **L-5 fixed** — the Page Pool table's unregistered `page_pool` panel was destroyed by the first grid render, its markup now lives inside the registered window with inner ids untouched; **L-7** (`test_title_fit.mjs`, `test_captcha_saved_page.mjs` now in `test:js`) and **L-8** (dead `WIN_ICONS`, hand-written id lists) closed; RED-first `tests/test_window_catalog.py` (7) + `tests/js/test_live_debug_panel.mjs` (6); pytest 1,765 · JS 269 · jscpd 1.086 %. I-51 added; SoR row 19 + module row. Next: S9 (live-debug content) — not started. Previously — **S7 landed** (`arena/01a0bfba-…`): every URL row that cannot receive a job right now shows a `⊘` after its status chip with the reason as tooltip (`row unchecked` / `no Chrome tab linked` / `tab not connected` / `tab busy with a job`); absence = healthy, the table keeps 8 columns. One owner: `live/url_policy.mark_receivers(rows, pool)` writes `UrlRow.receiver` (new field, appended last, default `False`, round-trips through `asdict` / `UrlRow(**u)`, `urls_to_js`, both undo builders) from `commit_urls` / `commit_urls_system` and after presence in every reconciler pass; `receiver_reason` reads `enabled_tab_ids` (source-locked, RULE 10); `debug_view.annotate_receivers` adds the tooltip at emit. JS reflects only: one conditional inside the single `rowHtml` template line (`render.js` 74 / 12 unchanged), `.url-not-receiver` in `arena.css`. RED-first `tests/test_url_receivers.py` (14, real Bridge undo/redo), `tests/js/test_url_list_receiver_icon.mjs` (6); pytest 1,757 · JS 253 · coverage 87.77 / 84.38 · jscpd 1.086 %. I-53 added; SoR rows 19 / 21; RULE 13 corollary. Next: S8 (16th window contract + L-5 rescue). Previously — **S6 landed** (`arena/01a0bfba-…`): the URL table follows Chrome in every run state at a user-set cadence. New `app/services/live/reconcile.py` (232 lines — `reconcile_loop` reads `url_reconcile_interval_ms` every pass, `reconcile_once` = the old `auto_scan_pass` body with the idle-only prune replaced, `LiveDeps` seam so `live/` never imports ui/browser, `Report`), `app/services/live/url_policy.py` (171 lines, 100 % covered — `REMOVAL_RULES` table of 4 `(reason, predicate)` pairs, live-job deferral, 2-miss hysteresis, bounded checkbox memory; `dedupe_rows` / `add_rows` moved out of `panels/url_queue`), `app/services/live/debug_view.py` (the one interval clamp 500…60000 + `cadence` on `progress_updated.live`). `browser_tabs.py` 544 → 458 lines (`live_deps`, `start_url_reconciler` from `main_window._build_ui`; nine plan/apply/prune helpers gone), `url_queue.commit_urls_system` (no undo — I-37), `app_settings.apply_url_interval` (clamp → `set_state` → `wake("interval")` → one line; no new slot, 135), `cdp.js` 15 s timer deleted, new `url-list/interval.js` control in the URL job-cycle bar (frozen url-list files untouched — D-12R). RED-first `tests/test_url_policy.py` (25), `tests/test_live_reconcile.py` (11, real Bridge), `tests/test_url_interval_setting.py` (11), `tests/js/test_url_interval_control.mjs` (7); pytest 1,743 · JS 247 · coverage 87.73 / 84.33 · jscpd 1.088 %. I-50 added; SoR rows 8 / 11 / 21; RULE 10 corollary. Next: S7 (receiver flag + ⊘ icon). Previously — **S5 landed** (`arena/01a0bfba-…`): new `app/services/live/supervisor.py` (181 lines) — `run_live` (plan → wait or pass → tail; ends only on Cancel / Stop-after; `🏁 Batch cancelled` / `🏁 Batch complete` markers kept, new `🏁 Pass complete — run stays live (N queued)`), `plan_pass` → `PassPlan` (fresh read every pass; reasons `no images` / `no tab` / `all cooling` / `cdp down` with `REASON_LINES`, throttled 5 min per reason + one on change), `set_run_state` (the ONLY `_run_state` writer, also writes the persisted `AppState.run_state` — L-2). `batch_orchestrator` 489 → 433 lines: `run_batch` / `_run_guarded` / `_cancel_batch` / `_crash_batch` / `_finish_batch` / `_abort_no_tab` gone, `run_pass(bridge, plan)` + `prepare_batch(bridge, plan)` (no re-snapshot), `pool_summary` public; `multi_page_dispatcher._finalize_batch` log + emits only (`max_func_loc` stays 29). `run_control`: Start while live ⇒ wake + `🟢 Run already live — queue re-checked (N queued)`, five slots route through `set_run_state`; `queue_scan.run_folder_ai_request` refuses only while an image is `processing` (`feed.PROCESSING_REFUSAL`). `core/run_scope.in_live_scope(img, max_attempts)` — a live run never ends, so `failed` rests at `settings.retries.max_attempts` (Retry / Reset → `pending` always run); `feed.queued_images` / `retry_cap` / `in_flight`. Goldens **byte-identical** under the new `RUNNERS["supervisor"]` (harness arms `_stop_after` after one pass — what the old one-batch run did on its own). RED-first `tests/test_live_supervisor.py` (12); 13 lifecycle assertions moved/renamed with reasons; pytest 1,694 · JS 240 · coverage 87.48 / 84.11 · jscpd 1.101 %. I-47 added; SoR rows 6 + §3; RULE 7 / 10 / 13 corollaries. Next: S6 (dynamic URLs). Previously — **S4 landed**: new package `app/services/live/` — `bus.py` (`LiveBus`: thread-safe `wake` / `wait` / `throttle` / `reasons`, one per bridge) and `feed.py` (`commit_queue` — the one queue-write funnel: recalc → save → undo-if-user → wake; `recover_stale_processing`; `clear_row_assignments`); the live eligibility rule lives in `core/run_scope.live_scope` / `LIVE_STATUSES` (runnable minus `processing`) and `feed.eligible_images` re-exports it (no third copy, per the merge-note). D-6R: `reset_image_state(img)` lost its selection parameter — every reset re-queues `pending` + `selected`, logs the count, wakes the bus, is undoable. 8 panel/worker/preset sites end in `commit_queue`; `run_state.schedule_batch` replaces the coroutine-name-sniffing tracker; `init_run_state` creates the bus / RLock / undo seam. RED-first tests `test_live_bus.py` (6), `test_live_feed.py` (10), `test_reset_requeues.py` (6, real Bridge); goldens byte-identical; jscpd 1.24 → 1.106 %; panels shrank (run_control 275→273, queue_scan 315→304, app_settings 359→358). I-49 / I-54 added, SoR rows 3 + 6. Next: S5 (always-live run). Previously — **S3 landed**: the captcha wait is bounded and the generation-timeout pause is capped by the one knob `watcher_captcha_timeout_sec` — new `app/core/pause_clock.py` (`PauseClock`, 55 lines), `policy.pause_cap_seconds` / `wait_reason` / `WaitDeadline`, `output_wait.WaitSpec.pause` (+`_check_timeout` reads `paused_elapsed`, stamps `paused_s`/`pause_note`), `cdp_arena/output._settle_timed` + `_timeout_text`, `captcha/service._wait_outcome` (`_manual_wait` 27 → 22 lines; new outcome `wait_timeout`: retryable, no penalty), `single_job_runner` installs/drops the clock with the settler (`_drop_wait_hooks`) and maps `wait_timeout` via a status→error lookup (CC 6 → 4). RED-first tests: `test_pause_clock.py` (8), `test_output_wait_timeout_pause.py` (5), `test_captcha_wait_cap.py` (6), `test_captcha_wait_reason.py` (4) + one `test_cdp_arena.py` charge test; `cooldown_service.py` and its never-gives-up test untouched; goldens byte-identical. RULE 20 amended, I-52 added, SoR rows 8/12. Next: S4 (not started). Previously — **S2 landed**: captcha scope is the Watcher switch — new `app/services/captcha/policy.py` (54 lines, 5 functions, the sole reader of `watcher_enabled`), five gate edits (`check_security`, `_handle_security` → `Skipped (Watcher off)`, `wait_for_output` settler, `handle_captcha` → `out_of_scope` via `_handle_captcha_scoped`, `_watcher_running` → policy), RED-first `tests/test_captcha_scope.py` + the D-23 counting test `tests/test_watcher_off_zero_activity.py` (OFF = all zeros, ON positive control); 21 existing captcha tests armed with `watcher_enabled=True` because the switch is now load-bearing (goldens byte-identical); RULE 20 amended, I-19/I-34 updated, I-48 added. Next: S3 (capped captcha pause). Previously — **S0 + S1 landed**: S0 baseline measured and recorded in `current/QUALITY_RECHECK.md` (1,612 py / 240 js green, coverage 86.89 / 83.21, the tracked runtime `config/*.json` + `.pyc` re-untracked per I-43, the `max_cog`-0 baseline and the sandbox `libGL` coverage gap named as environment facts); S1 fixed **L-1** (`page_pool.connect_page_pool` → `run_state.schedule_coro`, 2 lines) with the RED-first `tests/test_page_pool_join.py` and de-masked **L-6** in `tests/test_panel_browser_tabs.py`; SYSTEM_OF_RECORD row 11 updated. Next: S2 (captcha scope). Previously — **merge**: the two plan folders from branch `arena/01a0bc3b-…` (`e1e916b…11751e1`, plan only) merged into `arena/01a0bba4-…` on top of B10–B13; the plans are kept verbatim (RULE 17) and `archive/2026-09-20-dynamic-urls-and-worker-debug/merge-note.md` records the drift the stages must absorb — invariant renumbering I-39…I-46 → I-47…I-54, `core/run_scope.py` as the existing eligibility rule, 135 slots, floors 86.36/82.33, the re-measure list, and the still-open L-1/L-5/L-6/L-7/L-8; the same merge re-untracked the runtime `config/` files (a rotated 2Captcha key) that the owner's local merge `889aa59` had re-added (I-43). Same day, earlier — **round-2 rev 3** added `tdd-interfaces.md` to `archive/2026-09-20-dynamic-urls-and-worker-debug/` (per-stage interface splits, expected files/functions with measured size budgets, and the RED-first test branches — TDD, tests before code; still **plan only, no production code**; measuring every touched symbol against the gate's own per-file maxima forced three new decisions **D-25/D-26/D-27**, corrected rev-2's claim that `PauseClock` could live in `output_wait.py`, and surfaced three latent test-integrity defects **L-6/L-7/L-8**). Same day, earlier — **round-2** planning entry added for `archive/2026-09-20-dynamic-urls-and-worker-debug/` (URL update-interval setting, captcha ⇄ generation-timeout pause, URL-row receiver icon + reset re-queue, L-1 fix, and the new **Live Worker & Queue Debug** window; **plan only, no production code**; amends round-1 D-1/D-6/D-9 and merges both rounds into one **staged chain** (S1…S10, most important first, each stage separately committable/gate-green/shippable — the owner's rev-2 instruction; the earlier single-wave shape is preserved in git at `a5f69ee`); reports new latent defect **L-5** — the unregistered `page_pool` panel is destroyed by the first grid render, which is why the worker table is invisible today; owner corrections applied — **D-14R** the captcha pause *and* the wait are capped at the existing `watcher_captcha_timeout_sec` (at the cap the job fails honestly as `wait_timeout`; worst case per image `generation_timeout + captcha_cap`) and **D-23** Watcher OFF means zero captcha activity of any kind, proven by a counting test; **D-12R** the writable interval control lives in the URL List job-cycle bar (new `url-list/interval.js`) because every frozen `url-list/*.js` file has zero headroom, and **D-24** the staging rule — a stage's new `.js` files are complete inside it, since an integrator's `--record-baseline` freezes whatever exists at that moment; `current/SYSTEM_OF_RECORD.md` / `current/AGENT_RULES.md` stay untouched until the code lands — RULE 17). Same day, earlier — planning entry added for `archive/2026-09-20-live-processing-and-watcher-scope/` (live processing & Watcher scope, **plan only, no production code**; `current/SYSTEM_OF_RECORD.md` and `current/AGENT_RULES.md` are deliberately untouched until the change lands — RULE 17 updates docs in the same change as the code). Previous: 2026-10-02 — Captcha Watcher isolation + UI bugfixes (archive entry + `current/QUALITY_RECHECK.md` added; SYSTEM_OF_RECORD rows 12/19/20, I-29/I-34, storage + module tables updated). Previous: 2026-09-19 — R2: Area D integrated (recording milestones/labels, coverage 84.47 %/80.27 %, mutation tooling, gate hardening) — see `archive/2026-09-19-refactor-cycle-integration/design.md` §8. Previous: R1: legacy `Process Images in Areana/Old App/**` tree removed (948 files) and `.gitignore` cleaned; docs map + SYSTEM_OF_RECORD pointers updated. Previous: refactor cycle X (R0+A+B+C integrated; archive entry added) and before that Area A complete (R0–R12).*
