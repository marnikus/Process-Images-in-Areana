"""
Site Adapter — centralized selectors for arena.ai image generation (RULE 21, I-18).
Replaceable because webpage structure will change.

The SELECTORS map between the generated markers is maintained by
``tools/generate_selectors.py``: it extracts every selector chain the live
probes actually use (cdp_arena, output_probes, new_chat, cdp_client,
captcha_js, action-block defaults), classifies each primary per RULE 21
(semantic > structural > class fragment), and regenerates this block.
Probe chains stay literal in the probe sources (tests/js/*.mjs execute the
real strings, RULE 8); the pre-push check runs ``--check`` so the map can
never drift. To update selectors: edit the probe, run
``python tools/generate_selectors.py --write``.
"""
from .selector import SelectorObject
from typing import Dict

# >>> generated:SELECTORS begin
# Generated by tools/generate_selectors.py — do not edit by hand.
# Chains keep exact live order (primary = chain[0], fallbacks = rest):
# tests/js/*.mjs execute the real probe strings (RULE 8).
SELECTORS: Dict[str, SelectorObject] = {
    'prompt_textarea': SelectorObject(
        name='prompt_textarea',
        primary='textarea[name="message"]',
        fallbacks=['textarea[placeholder^="Describe"]', 'textarea[rows="1"]'],
        scope='form',
        mustBeVisible=True,
        mustBeEnabled=True,
        expectedCount=1,
        textCondition=None,
        textConditionType='equals',
        verification='after insertion, textarea.value equals expected prompt exactly (RULE 22 read-back)',
        evidence='arena_find; arena_insert; arena_verify_prompt; arena_page_ready; newchat_queries; block_defaults',
        lastVerified='2026-09-19',
        tier='semantic',
    ),
    'send_button': SelectorObject(
        name='send_button',
        primary='button[aria-label="Send message"]',
        scope='form',
        mustBeVisible=True,
        mustBeEnabled=True,
        expectedCount=1,
        textCondition=None,
        textConditionType='equals',
        verification='wait for disabled->enabled transition before click; state probe checks .disabled itself',
        evidence='arena_send_state; arena_page_ready; block_defaults',
        lastVerified='2026-09-19',
        tier='semantic',
    ),
    'send_button_click': SelectorObject(
        name='send_button_click',
        primary='button[aria-label="Send message"]:not([disabled])',
        fallbacks=['form button[aria-label="Send message"]', 'button[aria-label="Send message"]'],
        scope='form',
        mustBeVisible=True,
        mustBeEnabled=True,
        expectedCount=1,
        textCondition=None,
        textConditionType='equals',
        verification='click once; JS loop additionally requires visible + not disabled',
        evidence='arena_click_send; block_defaults',
        lastVerified='2026-09-19',
        tier='semantic',
    ),
    'file_input': SelectorObject(
        name='file_input',
        primary='input[type="file"]',
        scope='form',
        mustBeVisible=False,
        mustBeEnabled=True,
        expectedCount=1,
        textCondition=None,
        textConditionType='equals',
        verification='hidden input; CDP DOM.setFileInputFiles can set files',
        evidence='arena_page_ready; attach_defaults; block_defaults',
        lastVerified='2026-09-19',
        tier='semantic',
    ),
    'file_input_cdp': SelectorObject(
        name='file_input_cdp',
        primary='form input[type="file"][accept*="image"]',
        fallbacks=['input[type="file"][accept*="image"]'],
        scope='form',
        mustBeVisible=False,
        mustBeEnabled=True,
        expectedCount=1,
        textCondition=None,
        textConditionType='equals',
        verification='attach_image_cdp scope-first chain (form-scoped, accept-constrained)',
        evidence='attach_defaults',
        lastVerified='2026-09-19',
        tier='semantic',
    ),
    'output_region': SelectorObject(
        name='output_region',
        primary='div.no-scrollbar',
        fallbacks=['main'],
        scope=None,
        mustBeVisible=True,
        mustBeEnabled=False,
        expectedCount=1,
        textCondition=None,
        textConditionType='equals',
        verification='output observation container (or app shell) exists — readiness sanity check, the other four checks are the real gates',
        evidence='arena_page_ready',
        lastVerified='2026-09-19',
        tier='class-fragment',
    ),
    'security_dialog': SelectorObject(
        name='security_dialog',
        primary='div[role="dialog"][data-state="open"]',
        scope=None,
        mustBeVisible=True,
        mustBeEnabled=False,
        expectedCount=0,
        textCondition='Security Verification',
        textConditionType='contains',
        verification='if visible → USER_ACTION_REQUIRED, pause, show page to user (RULE 20)',
        evidence='arena_page_ready_dialog; detect_query; visible_query; continue_click_query; inject_query',
        lastVerified='2026-09-19',
        tier='semantic',
    ),
    'processing_spinner': SelectorObject(
        name='processing_spinner',
        primary='div.animate-spin',
        scope=None,
        mustBeVisible=True,
        mustBeEnabled=False,
        expectedCount=0,
        textCondition=None,
        textConditionType='equals',
        verification='spinner visible near model label = generating; wait for it to disappear',
        evidence='arena_generating; out_queries',
        lastVerified='2026-09-19',
        tier='class-fragment',
    ),
    'attachment_preview_image': SelectorObject(
        name='attachment_preview_image',
        primary='div.flex.flex-wrap.gap-2 img[alt]',
        fallbacks=['div.flex.flex-wrap.gap-2 img[src^="blob:"]', 'form img[src^="blob:"]'],
        scope='form',
        mustBeVisible=True,
        mustBeEnabled=False,
        expectedCount=1,
        textCondition=None,
        textConditionType='equals',
        verification='preview is new, visible, inside the active composer area',
        evidence='arena_verify_attach',
        lastVerified='2026-09-19',
        tier='semantic',
    ),
    'output_image': SelectorObject(
        name='output_image',
        primary='div.no-scrollbar img[src*=".r2.cloudflarestorage.com/"]',
        fallbacks=['div.no-scrollbar img[src*="messages-prod."]', 'div.no-scrollbar img[loading="lazy"].aspect-square', 'img.aspect-square.cursor-pointer', 'img.cursor-pointer', 'div.flex img[src*=".r2.cloudflarestorage.com/"]', 'main img[src*=".r2.cloudflarestorage.com/"]', 'div.no-scrollbar img[src^="https://"]', 'img.aspect-square.w-full', 'img[src*=".r2.cloudflarestorage.com/"]', 'img[src*="messages-prod"]', 'img.h-\\[50vh\\]', 'img.w-\\[50vh\\]', 'ol img[src^="https://"]', 'main img'],
        scope=None,
        mustBeVisible=True,
        mustBeEnabled=False,
        expectedCount=0,
        textCondition=None,
        textConditionType='equals',
        verification='capture all srcs before submit; new output must be new vs baseline (RULE 15)',
        evidence='out_list; block_defaults',
        lastVerified='2026-09-19',
        tier='class-fragment',
    ),
    'wait_output_default': SelectorObject(
        name='wait_output_default',
        primary='div.no-scrollbar img[src*=".r2.cloudflarestorage.com/"], div[data-testid="output"] img, div.no-scrollbar img, img.aspect-square.cursor-pointer, main img[src*=".r2.cloudflarestorage.com/"]',
        scope=None,
        mustBeVisible=True,
        mustBeEnabled=False,
        expectedCount=0,
        textCondition=None,
        textConditionType='equals',
        verification='WAIT_OUTPUT block default selector list (user-configurable, RULE 3)',
        evidence='block_defaults',
        lastVerified='2026-09-19',
        tier='class-fragment',
    ),
    'spinner_check_default': SelectorObject(
        name='spinner_check_default',
        primary='div:has-text("Processing"), div:has-text("Generating"), [data-state="loading"], .spinner, [aria-busy="true"]',
        scope=None,
        mustBeVisible=True,
        mustBeEnabled=False,
        expectedCount=0,
        textCondition=None,
        textConditionType='equals',
        verification='AWAIT_PROCESSING_IMAGE block default (Playwright :has-text parts)',
        evidence='block_defaults',
        lastVerified='2026-09-19',
        tier='semantic',
    ),
    'model_label': SelectorObject(
        name='model_label',
        primary='span.truncate',
        scope='div.flex.min-w-0.flex-1.items-center.gap-2',
        mustBeVisible=True,
        mustBeEnabled=False,
        expectedCount=0,
        textCondition=None,
        textConditionType='equals',
        verification='model/answer label span next to the spinner',
        evidence='out_queries',
        lastVerified='2026-09-19',
        tier='class-fragment',
    ),
    'model_label_scope': SelectorObject(
        name='model_label_scope',
        primary='div.flex.min-w-0.flex-1.items-center.gap-2',
        scope=None,
        mustBeVisible=True,
        mustBeEnabled=False,
        expectedCount=0,
        textCondition=None,
        textConditionType='equals',
        verification='closest() scope for reading the model label',
        evidence='out_closest',
        lastVerified='2026-09-19',
        tier='class-fragment',
    ),
    'message_list_reverse': SelectorObject(
        name='message_list_reverse',
        primary='ol.flex-col-reverse',
        scope=None,
        mustBeVisible=True,
        mustBeEnabled=False,
        expectedCount=0,
        textCondition=None,
        textConditionType='equals',
        verification='ol with flex-col-reverse → newest message renders first',
        evidence='out_queries',
        lastVerified='2026-09-19',
        tier='class-fragment',
    ),
    'message_container': SelectorObject(
        name='message_container',
        primary='div.flex.min-w-0.flex-1.flex-col.items-end, div.group, div.flex.flex-col, div[data-message-id]',
        scope=None,
        mustBeVisible=True,
        mustBeEnabled=False,
        expectedCount=0,
        textCondition=None,
        textConditionType='equals',
        verification='message bubble container holding the output image',
        evidence='out_closest',
        lastVerified='2026-09-19',
        tier='class-fragment',
    ),
    'message_container_plain': SelectorObject(
        name='message_container_plain',
        primary='div.flex.min-w-0.flex-1.flex-col.items-end, div.group, div.flex.flex-col',
        scope=None,
        mustBeVisible=True,
        mustBeEnabled=False,
        expectedCount=0,
        textCondition=None,
        textConditionType='equals',
        verification='message container fallback without data-message-id',
        evidence='out_closest',
        lastVerified='2026-09-19',
        tier='class-fragment',
    ),
    'new_chat_button': SelectorObject(
        name='new_chat_button',
        primary='a[href="/image/direct"]',
        fallbacks=['li[data-sidebar="menu-item"] a[href="/image/direct"]', 'a[data-sidebar="menu-button"][href="/image/direct"]'],
        scope='li[data-sidebar="menu-item"]',
        mustBeVisible=True,
        mustBeEnabled=True,
        expectedCount=1,
        textCondition='New Chat',
        textConditionType='contains',
        verification='click returns to a clean new chat',
        evidence='newchat_candidates',
        lastVerified='2026-09-19',
        tier='semantic',
    ),
    'recaptcha_iframe': SelectorObject(
        name='recaptcha_iframe',
        primary='iframe[title="reCAPTCHA"], iframe[src*="recaptcha"]',
        scope=None,
        mustBeVisible=True,
        mustBeEnabled=False,
        expectedCount=0,
        textCondition=None,
        textConditionType='equals',
        verification='challenge iframe — badge anchor excluded by geometry + closest(.grecaptcha-badge)',
        evidence='detect_consts; visible_consts',
        lastVerified='2026-09-19',
        tier='semantic',
    ),
    'recaptcha_widget': SelectorObject(
        name='recaptcha_widget',
        primary='iframe[title="reCAPTCHA"], iframe[src*="recaptcha"], div.recaptcha-v2-container, #recaptcha-v2-container',
        scope=None,
        mustBeVisible=True,
        mustBeEnabled=False,
        expectedCount=0,
        textCondition=None,
        textConditionType='equals',
        verification='any reCAPTCHA widget (badge anchor or challenge frame)',
        evidence='detect_consts',
        lastVerified='2026-09-19',
        tier='semantic',
    ),
    'recaptcha_response_field': SelectorObject(
        name='recaptcha_response_field',
        primary='textarea[name="g-recaptcha-response"], textarea[id*="g-recaptcha-response"]',
        scope=None,
        mustBeVisible=False,
        mustBeEnabled=False,
        expectedCount=0,
        textCondition=None,
        textConditionType='equals',
        verification='token field read-back (verify the solve token landed)',
        evidence='detect_query',
        lastVerified='2026-09-19',
        tier='semantic',
    ),
    'recaptcha_response_field_inject': SelectorObject(
        name='recaptcha_response_field_inject',
        primary='textarea[name="g-recaptcha-response"], input[name="g-recaptcha-response"], #g-recaptcha-response',
        scope=None,
        mustBeVisible=False,
        mustBeEnabled=False,
        expectedCount=0,
        textCondition=None,
        textConditionType='equals',
        verification='token fields written by the inject probe (every field)',
        evidence='inject_consts',
        lastVerified='2026-09-19',
        tier='semantic',
    ),
    'grecaptcha_badge': SelectorObject(
        name='grecaptcha_badge',
        primary='.grecaptcha-badge',
        scope=None,
        mustBeVisible=False,
        mustBeEnabled=False,
        expectedCount=0,
        textCondition=None,
        textConditionType='equals',
        verification='badge identity exclusion — never a visible challenge',
        evidence='detect_closest; visible_closest',
        lastVerified='2026-09-19',
        tier='class-fragment',
    ),
    'captcha_integration_script': SelectorObject(
        name='captcha_integration_script',
        primary='script[src*="enterprise"], script[id*="recaptcha-enterprise"]',
        scope=None,
        mustBeVisible=False,
        mustBeEnabled=False,
        expectedCount=0,
        textCondition=None,
        textConditionType='equals',
        verification='enterprise vs unknown integration detection',
        evidence='detect_query',
        lastVerified='2026-09-19',
        tier='structural',
    ),
    'captcha_image': SelectorObject(
        name='captcha_image',
        primary='img[src*="captcha"], img[alt*="captcha" i]',
        scope=None,
        mustBeVisible=True,
        mustBeEnabled=False,
        expectedCount=0,
        textCondition=None,
        textConditionType='equals',
        verification='image CAPTCHA inside the dialog',
        evidence='detect_query',
        lastVerified='2026-09-19',
        tier='structural',
    ),
    'sitekey_attr': SelectorObject(
        name='sitekey_attr',
        primary='[data-sitekey]',
        scope=None,
        mustBeVisible=False,
        mustBeEnabled=False,
        expectedCount=0,
        textCondition=None,
        textConditionType='equals',
        verification='sitekey read — dialog-scoped first, then document',
        evidence='detect_query',
        lastVerified='2026-09-19',
        tier='structural',
    ),
    'add_files_button': SelectorObject(
        name='add_files_button',
        primary='button[aria-label="Add files"]',
        scope='form',
        mustBeVisible=True,
        mustBeEnabled=True,
        expectedCount=1,
        textCondition=None,
        textConditionType='equals',
        verification='click triggers file chooser (ATTACH_IMAGE click_selector)',
        evidence='block_defaults',
        lastVerified='2026-09-19',
        tier='semantic',
    ),
    'attachment_preview_block': SelectorObject(
        name='attachment_preview_block',
        primary='div.flex.flex-wrap.gap-2 img',
        scope='form',
        mustBeVisible=True,
        mustBeEnabled=False,
        expectedCount=1,
        textCondition=None,
        textConditionType='equals',
        verification='VERIFY_ATTACHMENT block default',
        evidence='block_defaults',
        lastVerified='2026-09-19',
        tier='class-fragment',
    ),
    'security_check_default': SelectorObject(
        name='security_check_default',
        primary='div[role="dialog"][data-state="open"], iframe[title*="reCAPTCHA"], div:has-text("Security Verification"), div:has-text("captcha"), [data-testid="security-dialog"]',
        scope=None,
        mustBeVisible=True,
        mustBeEnabled=False,
        expectedCount=0,
        textCondition=None,
        textConditionType='equals',
        verification='CHECK_SECURITY block default (Playwright :has-text parts, user-configurable)',
        evidence='block_defaults',
        lastVerified='2026-09-19',
        tier='semantic',
    ),
    'recaptcha_dialog_scope': SelectorObject(
        name='recaptcha_dialog_scope',
        primary='div[role="dialog"]',
        scope=None,
        mustBeVisible=True,
        mustBeEnabled=False,
        expectedCount=0,
        textCondition=None,
        textConditionType='equals',
        verification='dialog scope for token-field scoping in the inject probe',
        evidence='inject_closest',
        lastVerified='2026-09-19',
        tier='semantic',
    ),
    'recaptcha_callback_attr': SelectorObject(
        name='recaptcha_callback_attr',
        primary='[data-callback]',
        scope=None,
        mustBeVisible=False,
        mustBeEnabled=False,
        expectedCount=0,
        textCondition=None,
        textConditionType='equals',
        verification='data-callback on the widget — solve-callback search',
        evidence='inject_query',
        lastVerified='2026-09-19',
        tier='structural',
    ),
    'recaptcha_anchor_iframe': SelectorObject(
        name='recaptcha_anchor_iframe',
        primary='iframe[src*="recaptcha"]',
        scope=None,
        mustBeVisible=True,
        mustBeEnabled=False,
        expectedCount=0,
        textCondition=None,
        textConditionType='equals',
        verification='anchor iframe src pattern',
        evidence='inject_query',
        lastVerified='2026-09-19',
        tier='structural',
    ),
}
# <<< generated:SELECTORS end

def get_selector(name: str) -> SelectorObject:
    if name not in SELECTORS:
        raise KeyError(f"Selector {name} not found")
    return SELECTORS[name]
