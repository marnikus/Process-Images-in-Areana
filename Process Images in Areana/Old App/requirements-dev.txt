# Development-only tools. Nothing here is needed to run the bot —
# `pip install -r requirements.txt` is enough for that.
#
#   pip install -r requirements-dev.txt
#
# These are the tools the code-quality gate named in
# docs/archive/2026-09-10-quality-gates/RULE16_SIZE_COMPLEXITY_FIT_2026-09-10.md measures with. The gate
# (tests/test_rule16_new_code.py) skips the complexity and smell checks when
# they are absent rather than pretending to pass.
#
# Versions are pinned to the ones this gate was verified against.

-r requirements.txt

# cyclomatic complexity (radon counting rules)
radon==6.0.1
# SonarSource cognitive complexity
cognitive-complexity==1.3.0
# dead code
vulture==2.16
# duplicate-code (R0801) and unused-import checks
pylint==4.0.8
# coverage with branch analysis
pytest-cov==7.1.0
# mutation testing (RULE 16 wants >= 70% mutation score on new code).
# Configured in setup.cfg; see docs/archive/2026-09-10-quality-gates/RULE16_SIZE_COMPLEXITY_FIT_2026-09-10.md §8
# for what it can and cannot see.
mutmut==3.7.0
# the test runner itself
pytest==9.1.1
pytest-asyncio==1.4.0
# parallel execution — from 387 s to ~100 s with -n auto
# Tiered runner (tools/metrics/run_tiers.py) uses it for pure+db tiers;
# Qt tier stays single-process (QApplication singleton not fork-safe).
pytest-xdist==3.8.0
# Runs the RULE 16 gate before a commit lands. Install the hook once with
# `pre-commit install`; the config is .pre-commit-config.yaml. Without this
# package the documented setup (`pip install -r requirements-dev.txt` then
# `pre-commit install`) fails at the second step.
pre-commit==4.6.2
